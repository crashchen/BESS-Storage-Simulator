# 第二、三批：用户提供的 CC 复审与收尾

日期：2026-09-19。基线 `053c82c`。本代理没有调用或调度 CC。

## 复审结论与证据归属

用户提供的完整 CC 报告认为两批可合并、没有阻塞问题。CC 报告自行通过 lint/test/build（192 测试 / 17 文件），复现全部 12 个收敛场景，另做 972 组边界/退化组合、逐子段记账与真实浏览器状态检查。这些额外检查归属于用户提供的 CC 报告，不冒称本代理独立运行了其未提供的探针。粘贴报告部分表格被截断，不补造缺失数值。

CC 未能改变 Chrome CSS 视口，390px 容器压缩检查不能替代真实手机视口；本代理此前 320/390 CSS 视口证据与 CC 的检查分开保留。CC 仅验证组合 192/17，第二批单独 175/16 是阶段历史。

## 已落实的小项

- `Night reserve 40%` 仅 AUTO 显示，手动模式不再显示该专有目标；在已有手动状态测试中补断言。
- `selectBessPower` 共享原有 0.05 MW 展示阈值；场景仅取功率状态，不生成策略/原因字符串，完整 selector 复用同一入口。
- `simulateTick` 注释记录每段消耗正时间、到达边界后转移被禁用或目标改变的进展约束；不增加会静默丢弃剩余时间的迭代截断。

这些收尾不修改调度策略或估值公式。重新执行 lint/test/build，192/17；Three vendor 724.90 kB 提示仍在。收敛脚本重新生成证据哈希，数值算法未变。

## 独立复算：多日 AUTO 产品事项

用同源打包的基线和工作树，从默认 08:00 / 65% 开始各连续跑 5 天，真实步长 0.01s、1440×，精确结束在 18:00 / 23:00。744 MWh 在第二至第五天的 18:00 SoC 均为 0、峰段放电为 0；两版一致。2400 MWh 第三至第五天仍有峰段输出。原因是 06–18 时自消纳可放至 0，12% reserve 只约束峰段，不提前为峰段保留电量。

能量口径应标明：当前代码 744 MWh 首日交流侧峰段输出约 149.1814 MWh，内部储能下降约 155.3973 MWh；2400 MWh 稳态交流侧约 210.1355 MWh，内部约 218.8912 MWh（放电效率 96%）。CC 的 155.4 / 218.9 与内部能量减少量一致。峰段零放电不等于全天零收益。

独立产品待办：比较「白天自消纳优先」「预留峰段能量」「提高夜间目标」的多日行为与假设，再选策略。默认先不改 40% / 12% 或增加价格门。若每日峰段套利是演示必达目标，此事项应优先；它不是本批数值回归，不与第四批加载/发布工程混在一起。
