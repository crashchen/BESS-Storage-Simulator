# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev         # Vite dev server
npm run build       # Type-check (tsc -b) then Vite production build
npm run lint        # ESLint across the repo
npm run test        # Vitest (single run, jsdom env)
npm run test:watch  # Vitest in watch mode
npm run preview     # Preview the production build
```

Run a single test file or name:

```bash
npx vitest run src/utils/simulationModel.test.ts
npx vitest run -t "active-power"
```

CI (`.github/workflows/ci.yml`) runs `lint → test → build` on pushes to `main` and PRs to any base branch, so `npm run build` must succeed — type errors break the pipeline, not just `lint`. Deploy workflow publishes to GitHub Pages; `vite.config.ts` reads `BASE_URL` from env for that base path.

## Architecture

The app is a single-page React + Three.js simulator. There is **no backend** — simulation state lives in-memory in `useGridSimulation`, with separate drawer and viewport presentation state, rendered by a 3D layer (r3f) and a 2D HUD layer (Tailwind panels).

### The simulation loop is the spine

Everything flows from `src/hooks/useGridSimulation.ts`. Key points that aren't obvious from the file tree:

- **Ref-based tick, throttled React updates.** The `requestAnimationFrame` loop mutates `simRef.current` every frame but only calls `setState` every `SIMULATION.renderSyncIntervalMs` (~33 ms / 30 fps). Chart snapshots are pushed on a separate `SIMULATION.snapshotIntervalMs` cadence into `historyRef`. If you add state consumed by the UI, read it from `state` (throttled) — do not subscribe components to `simRef` directly.
- **`dispatch(cmd)` is the only simulation write path.** Simulation controls produce a `BESSCommand` union (`src/types.ts`) handled by `applyCommand()` in `src/utils/gridReducer.ts`. The reducer is pure — it returns a `ReducerResult` containing the next `GridState` plus a `sideEffects` manifest (reset history, reset timer refs, etc.) that the hook applies. New simulation controls should add a command variant to the union, not mutate state ad hoc. Drawer and equipment-selection state belong to the presentation layer.
- **Explicit Reset also resets tariff drafts.** The hook increments `simulationResetVersion` only for `RESET_SIMULATION`, including consecutive resets with unchanged model values. This presentation signal stays outside `GridState`; App passes it through required `ControlPanelProps` and EconomicsPanel props. Pause/Stop do not emit it. Tariff drafts are discarded when either their source price or this version changes; do not broaden this contract into an assumed reset of every numeric field.
- **Demo scenarios are temporarily hidden from the main UI.** Presets still live in `src/scenarios.ts` and can be applied through `APPLY_SCENARIO_PRESET` for tests or future restoration. Do not re-enable the scenario panel until each preset has been revalidated against the active-power settlement model.
- **`simulateTick` lives in `src/utils/tickEngine.ts`.** Given `(prev, dtReal, now)` it returns the next `GridState`. It handles clock/SoC/strategy event sub-stepping, dispatch logic (`auto` vs. manual charge/discharge/idle), SoC clamping, efficiency losses, and PCC import/export settlement. The tick is fully deterministic — there is no injected random source. The settlement and demo-value math is delegated to `src/utils/simulationModel.ts` so it can be unit-tested without the RAF loop — add new simulation math there, not inline in the hook.
- **Selectors** in `src/utils/gridSelectors.ts` compute derived values (`selectBatteryDurationHours`, `selectGridConnectionTotalMw`). The physical `getBatteryTransferLimitMw` helper lives in `src/utils/simulationModel.ts` and is shared by the reducer, tick engine, and UI.
- **Display-only flow decomposition** in `src/utils/energyFlowTelemetry.ts` turns `GridState` into the 7 visible energy-flow legs (`solarToBess` / `solarToLoad` / `solarToExport` / `bessToLoad` / `bessToExport` / `gridToBess` / `gridToSite`). The 3D scene and any flow UI consume from here, not directly from raw state. `src/utils/sceneAssetInfo.ts` supplies the structured payload for the hover/click info card on BESS / PCS-MV / Grid Node.
- **BESS presentation** in `src/utils/bessDisplay.ts` separates sampled power, simulation run state, selected dispatch policy, and provable limits. Do not infer actual action from `batteryMode`: manual commands preseed it and paused edits may leave it stale. Pause and scenario presets can retain a nonzero frozen power sample; do not change the reducer or zero that sample to simplify display wording.

### Config is the single source of truth

`src/config.ts` centralises every tunable number (solar shape, BESS limits, tariff windows, active dispatch targets, 3D scene params). `useGridSimulation` and `simulationModel` both import from it — never hard-code magic numbers in components or hooks; add them to the appropriate `as const` block in `config.ts`.

### Two demo-value accumulators, intentionally different

`cumulativeRevenueEur` (Project demo value) and `cumulativeBessMarginEur` (BESS demo margin) are computed separately by `settleHybridProjectTick` in `simulationModel.ts`:

- **Project demo value** = direct PV exports + BESS discharge value − grid-paid BESS charging. It treats `Solar → BESS` as zero-cost.
- **BESS demo margin** = discharge value − grid charge cost − *opportunity cost of `Solar → BESS`* (solar that could have been sold now but was stored instead).

This split is a product decision, not a bug. Don't "simplify" them into one number. The explanation is surfaced to users in `EconomicsPanel.tsx` and noted in the README — keep wording consistent if you touch it.

All BESS discharge is valued at the settlement tariff, including local supply that restores otherwise unserved demand at the PCC limit. `cumulativeBessDischargeRevenueEur` is a legacy-named aggregate retained for historical result continuity; it equals the sum of `cumulativeBessExportRevenueEur`, `cumulativeBessAvoidedImportCostEur`, and `cumulativeBessRestoredLoadAssumedValueEur` within floating precision. Compute avoided imports by comparing capped actual imports with the same-tick no-BESS baseline; any local BESS supply beyond that reduction is restored load. `gridOverloadMw` is unserved demand after capped actual imports. The restored-load row is a tariff-based assumption, not revenue or an actual import saving; negative prices can make it negative. Both demo totals include it. Preserve the old total formulas and show the assumed amount even when current overload returns to zero. These totals are not realized cash flow or a full financial forecast.

`SOLAR.yieldKwhPerKwYear` is an annual reference only. It does not calibrate the current daily cosine curve or create an annual forecast. Do not describe the curve as modelled from this reference or measured site data.

### Active-power dispatch model

The live simulator is currently scoped to **Energy Arbitrage + Self-consumption**. It does not model FCR, frequency response, voltage control, protection trips, or AC transients. The frequency surface (state field + config constants) has been removed; do not reintroduce it without first deciding what behaviour the new fields should drive.

`tickEngine.ts` uses a fixed pipeline:

1. **Intent** — `dispatchMode` selects `auto`, `manual-charge`, `manual-discharge`, or `manual-idle`.
2. **Hardware clamp** — BESS power is limited by PCS rating, BESS grid connection, SoC, energy capacity, and efficiency.
3. **PCC settlement** — `settleHybridProjectTick` balances demand, PV, BESS, grid import/export, curtailment, and overload.

`AUTO` uses a simple rule tree: discharge during peak (paced as `usableEnergy × η_d / remainingPeakHours`); lock out automatic off-peak discharge; charge toward 40% SoC during off-peak; charge from PV surplus. In the shoulder window, keep that target only when the edited peak tariff clears the estimated efficiency, peak-pacing and reserve-value hurdle in `autoPolicy.ts`, using only energy above it for local demand. Otherwise retain shoulder self-consumption down to 0%. This is a steady-cycle tariff heuristic, not a forecast or a guarantee of better value under every load/PV/PCC condition. The rule tree still drops the old forecast planner's symmetric round-trip price gate for peak discharge itself — peak windows discharge whenever SoC > the 12% reserve, regardless of the off-peak / peak rate ratio. That contract is locked in by `tickEngine.test.ts`'s "AUTO discharges through peak regardless of unfavorable tariff ratios" case; revive a full price optimizer only after a deliberate product decision. If PV plus BESS export would exceed the PCC limit, PV export keeps priority and BESS discharge is reduced before curtailment. Manual modes bypass economic rules but still respect physical/PCC constraints.

`tickEngine.ts` now settles to energy events before recomputing the remaining interval: 0/100% SoC, the off-peak 40% target, the conditional shoulder 40% floor, and the applicable peak reserve. Trial PCC settlements use dt=0; only accepted segments book energy/money. Root search must use settled power and re-sample its midpoint, not divide headroom by the unconstrained PCS request. Clock splits include midnight (demand wrap) and the peak horizon-floor start. Within the floor, integrate the existing capped/exponential pacing law; do not hold the initial rate throughout the interval. Solar/demand remain midpoint approximations, not exact integrals.

Instantaneous telemetry remains the final settled segment, while cumulative fields sum all segments. A tick ending exactly at full/empty can retain a nonzero final sample; a tick crossing it can finish idle after booking transfer in earlier segments. Do not reconstruct whole-tick energy by multiplying final power by outer dt. `tickConvergence.test.ts` observes accepted settlements to check source/destination energy and all cost components, with fixed-horizon convergence and analytic pacing cases. See `docs/audits/2026-09-19/batch-3.md` and its before/after evidence script.

The dispatch API is converged on `SET_DISPATCH_MODE` (`payload: DispatchMode`). The `CHARGE` / `DISCHARGE` / `IDLE` shortcuts are kept as UI-friendly sugar; `gridReducer.test.ts` has an `it.each` test pinning that `SET_DISPATCH_MODE` and the shortcuts produce identical state for every manual mode.

All simulation writes use `BESSCommand`; only the appropriate dispatch/reset/preset operations change dispatch intent. `PAUSE_SIMULATION` preserves the current run. `STOP_SIMULATION` creates a fresh stopped run, restores time and SoC to their initial values, and clears accumulated results/history while preserving capacities, tariffs, demand scale, time speed, and dispatch intent. `RESET_SIMULATION` also restores those settings to the baseline. Keep these distinct semantics in the UI and documentation.

### Rendering layers

`App.tsx` composes three overlays over a fullscreen container:

1. `SimulationViewport` — r3f `Canvas` wrapped in a `CanvasErrorBoundary`, hosting `MicrogridScene` (generic BESS / PCS-MV / Grid Node GLB equipment with a dynamic BESS SoC overlay, solar array, energy-flow particles, curtailment particles, overload highlighting, and time-of-day lighting). Simulation data comes from `GridState` props; hover, selection, and viewport failure are local presentation state. Retry remounts the canvas without resetting the simulation. Render-error retries clear each equipment URL cache key through `equipmentModels.ts` before remount; context-loss retries retain successfully loaded models.
2. `StatusHud` — compact top-of-screen live metrics bar.
3. `ControlPanel` — left/right slide-out drawers controlled by `App` through `useDrawerLayout`. Entering the compact breakpoint retains the most recently opened drawer. Equipment cards are hidden while Metrics is open, or either drawer is open on compact screens; the pinned selection is retained while hidden. Explicit equipment inspection closes the drawers. Inside, individual panels live under `src/components/panels/` and share primitives (`Gauge`, `ActionButton`, `NumericField`, `PanelCard`) from `src/components/ui/PanelPrimitives.tsx`. `RecoverableTelemetryChart` lazily loads the chart with a local error boundary and fresh URL retry. The root `AppErrorBoundary` is only a last-resort render fallback; its explicit reload loses the run.

`DrawerSide`, `DrawerState`, `DrawerLayout`, and `ControlPanelProps` live in `src/types.ts`; hooks and components consume these contracts, without a reverse dependency from types to hooks. `layout` and `simulationResetVersion` are required ControlPanel props. `close-all` returns the same state when both drawers are already closed. Viewport hover always records pointer enter/leave; only card/highlight display is gated by drawer visibility, so closing a drawer restores the current hover and cannot revive one that left while hidden. An unpinned hover-preview card is pointer-transparent: it can open over the hovered equipment (the Grid transformer and label on desktop, the scene centre below `lg`), and catching the pointer there made hover flicker and swallowed the click. Only a pinned card, which has the Close control, takes pointer input.

### Bundle splitting

`vite.config.ts` defines manual `rollupOptions.output.manualChunks` that split `react`, `three`, `@react-three/fiber`, `@react-three/drei` (+ three-stdlib/camera-controls/meshline), `recharts`/`d3`, and `three/examples` into separate vendor chunks. Troika, bidi-js, and webgl-sdf-generator have their own `troika-vendor` chunk. `build/telemetryChunk.ts` emits the chart entry URL. Chart-only code shares that single module so a new retry query avoids failed browser module-map entries; shared formatTime stays in app-shared. The plugin rejects builds if the chart becomes eager or gains an unloaded static dependency. Successful chart imports are reused across drawer remounts. Keep Recharts in `optimizeDeps.include`: the opaque URL bypasses Vite scanning, and late dependency discovery would reload the dev page and lose the run. Splitting other vendor chunks does not itself make them lazy-loaded. If you add a heavy dependency, consider whether it needs its own chunk here — otherwise it lands in the app bundle.

### R3F scene patterns

`MicrogridScene.tsx` (~1,200 lines) contains many sub-components (BESSContainer, SolarPanel, EnergyParticle, CurtailmentParticle, etc.). Key patterns to follow when editing:

- **Avoid mixing declarative JSX material props with imperative `useFrame` updates on the same property.** R3F's reconciler may overwrite your `useFrame` changes on re-render. If you animate a property in `useFrame`, don't also set it via JSX.
- **Reuse Vector3 and Color objects** — pass a target ref to `curve.getPoint(t, targetVec)` instead of allocating each frame.
- **`useMemo` dependencies on floating-point values** like `soc` should be discretized (e.g., `Math.round(soc / 100 * 8)`) since the raw float changes every frame, making the memo pointless.
- **Scene labels stay pointer-transparent.** Drei `Html` mounts each label inside R3F's event source, so a label that took DOM pointer events would hand R3F label-relative offsets and break canvas raycasts. To make a label selectable, pass `hitArea` to `SceneLabel`: an invisible child of the equipment group resolves rays through the label's rendered rectangle to that group's existing handlers (`sceneLabelHitArea.ts`, distance 0 so the overlay wins over geometry behind it). Only the offset GRID NODE label uses it; BESS/PCS labels still pass input to the body below.
- **Solar supports stay upright.** Only the panel frame and active surface share the tilted group. The pole is a sibling outside that rotation, with its height derived from the panel position and ground height.
- **Equipment GLBs** live in `public/models` (metre-scale, centre-ground anchor, texture-free; provenance in `public/models/README.md`), are configured in `SCENE_3D.models`, and load via drei `useGLTF` at `import.meta.env.BASE_URL + file` so the GitHub Pages sub-path keeps resolving. Never mutate GLB materials for highlight/overload feedback — use separate overlay meshes (the cached GLTF must stay pristine across canvas remounts). `modelAssets.test.ts` pins the files as valid glTF 2.0 binaries free of the banned brand tokens. Each model is a representative single unit for station-scale equipment — keep info-card wording aggregate-framed (`sceneAssetInfo.ts` derives the "≈N × unit" equivalence from `unitRatingMw` / `unitEnergyMwh`), and don't present single-unit ratings as station capacity. The generic BESS is derived without touching Switchyard by `scripts/make_generic_bess_glb.py`; retain the dynamic SoC and interaction overlays in scene code rather than modifying its cached GLB materials.

### Tests

Vitest runs in `jsdom` (`vitest.config.ts`) with `src/test/setup.ts` and a `makeGridState` fixture in `src/test/fixtures.ts` for building partial `GridState` objects. The pure settlement math in `src/utils/simulationModel.ts`, command handling in `gridReducer.ts`, and tick behavior in `tickEngine.ts` all have dedicated tests; UI tests (`*.test.tsx`) use React Testing Library. `PanelPrimitives.test.tsx` covers `ActionButton` (aria-pressed) and `NumericField` (input validation, error states). When adding simulation behavior, prefer extending `simulationModel.ts` / `tickEngine.ts` with unit tests over testing through the RAF loop.

The 2026-09-19 combined second/third batches on `codex/audit-batch-2-model-clarity` pass lint/build and 192 tests in 17 files; the user supplied a passing CC review, and its two presentation nits plus loop-progress documentation are addressed. See `docs/audits/2026-09-19/cc-review.md`. Second-batch-only checks were 175/16. The first batch (146/15) was merged through PR #4 as `053c82c`; CI run 35449398794 and Pages run 35449398727 succeeded. `EconomicsPanel.test.tsx` includes a real ControlPanel/simulation-hook integration case for consecutive same-value Resets, preserved input identity, and Reset focus. Removing the hook's reset signal makes that regression fail with expected 350, actual 2000. The existing `ControlPanel.test.tsx` wrapper intentionally owns drawer state and supplies reset version 0, using `Omit` of the central props contract. It remains useful for focused component tests.

`bessDisplay.test.ts` and its consuming UI tests cover paused presets, actual reducer edits and tick samples, stale legacy modes, final nonzero SoC-boundary samples, display rounding, and provable zero-power conditions. Economics tests preserve the cumulative caveat after overload clears with details closed; settlement regressions pin the restored-supply assumption at positive and negative tariffs. The one-decimal display deadband is 0.05 MW; it does not change the engine's 0.01 MW mode threshold or power precision. See `docs/audits/2026-09-19/batch-2.md` for the current review scope and browser evidence.

`App.test.tsx` covers hidden hover entry, immediate restoration on drawer close, and hidden pointer leave, plus retry state preservation under StrictMode with the real hook/reducer and a simulated canvas context-loss event. These event tests are not browser injection of a stationary pointer or an actual GPU failure. First-batch real-browser checks at 1280×720 verified upright solar supports and 350 → invalid 2000 → Reset → 350 with cleared validation and focus on Reset; desktop/phone layout and tariff-timeline checks remain valid. The Three vendor chunk warning remains at 724.90 kB. Remaining product/experience follow-ups are tracked in `docs/audits/2026-09-08/README.md`.


### Fourth-batch verification (updated 2026-09-20)

Batches 2–3 (`33ed745`, PR #5) and batch 4 (`9926ad7`, PR #6) were merged on September 20; that release reached production as `be9eb17`, with CI 35504681930 and Pages 35504682035 successful. Batch 4 on `codex/audit-batch-4-recovery` passed the user-arranged CC review, with the loader regression gap and non-nvm setup instructions addressed afterward. It was reviewed against PR #5, then retargeted to main and merged after #5. Node 24.21.0 is pinned in `.nvmrc`; activate it with an available version manager or a local official distribution on PATH, then run `npm ci` (see README; nvm is optional). CI reads the same file. The deploy workflow calls reusable CI quality and publishes that run's verified dist artifact, never an independent unchecked build.

September 20 Node 24 lint, 197 tests / 18 files, Pages-path build and actionlint passed. September 19 clean install and both npm audit scopes passed with zero advisory entries (not a security proof); Three 724.90 kB warning remains. Browser HTTP 503 tests cover chart retry and repeated GLB failure/recovery without simulation reset. Keep these separate from the mocked GPU context-loss tests. The batch 4 session could not obtain a 390px CSS viewport; batch 5 separately succeeded at 320/390/768/1024/1280 widths, without reclassifying the older evidence. See `docs/audits/2026-09-19/batch-4.md` for evidence, limitations and the reproducible fault server. Do not invoke CC automatically.

The inactive PerformanceMonitor/AdaptiveDpr pair was removed; DPR remains capped at 1–2 with no FPS adaptation. Multi-day AUTO reserve policy is an independent product decision: default 744 MWh can run out before later-day peak windows; no strategy change belongs to this loading/release batch.

`Recovery.test.tsx` now renders the default production chart loader without a `load` prop, replacing only `importTelemetryChart.ts` (native import transport) and the virtual asset URL. It pins Pages-path/query preservation, attempts 0/1/2 after repeated failure, latest history, and success reuse across component remounts. Removing the attempt query was verified to fail this test. These tests do not emulate browser module-map caching; the separate real HTTP 503 evidence remains necessary. PR CI accepts any target branch so stacked batch reviews receive checks. See `docs/audits/2026-09-19/batch-4-review.md`.

### Fifth-batch scene access (2026-09-20; pushed 2026-09-22)

`codex/audit-batch-5-scene-access` starts at deployed `be9eb17`; commit `e345c1f` is pushed as PR #7 and CI 35699339050 passed. Its local lint, 214 tests / 21 files and Pages-path build pass. No simulation strategy or accounting changes. The Three chunk is 724.91 kB with its warning retained. See `docs/audits/2026-09-20/batch-5.md` for browser evidence and review scope; keep this batch distinct from the previously shipped 197/18 suite.

`SceneCameraControls` replaces the scene's bare OrbitControls. It fits `SCENE_3D.framing` at mount or an explicit `viewResetVersion` change. With batch 6, resize re-fits only until the user manually changes the camera; both resize and ordinary simulation updates must preserve an inspected view. The configured envelope includes the maximum 6×5 PV array, all representative equipment, labels and pylons; update it when site layout changes. Do not derive the fit from the decorative 200m ground or moving particles. Tests project all eight corners through a real Three camera and exercise the controller with real OrbitControls. Portrait fits can exceed the old 50m maximum, so the control limit and fog start are adjusted together. Native Three objects are mutated in the effect through R3F's get(), outside React render.

Equipment shares `SCENE_3D.equipmentScale = 0.9`; per-model scale fields have been removed. The PCS-MV skid is about the same size as the BESS container, not a small cabinet. Resize pads and reposition equipment when needed; do not independently shrink a unit to fit. GLB trim can extend beyond the nominal `size`, so verify actual geometry bounds. The PCS flow waypoint follows the pad top plus scaled height and clearance; BESS/local-load bus waypoints keep curved paths clear of the skid. Transformer placement is in config, with its pylon and flow ports derived from its position and scaled dimensions. The default viewing direction is more frontal and the curated envelope extends to [15.7, 5.6, 3.5]. These are representative visual placements, not electrical installation clearances.

Scene tools are native DOM buttons, hidden while either drawer is open and disabled during viewport failure. Equipment selection shares the existing pinned card/highlight. A toolbar selection requests focus on the card close button; Escape/Close returns to that equipment button. Temporarily hiding a pinned card for Metrics does not request focus again. The card reserves bottom space for the toolbar at all breakpoints, with a centered layout below lg; on desktop its top is 160px and its max height subtracts 256px so both the Metrics handle and bottom toolbar stay clear; scrollable regions use dark thin scrollbars. Full site clears inspection and resets only the camera request, never simulation state or the Canvas. `h-dvh` follows the available viewport height.

CC reviewed the 208/20 fifth-batch worktree; the final local commit passes 214/21. `EquipmentModel.tsx` is the unchanged shared GLB renderer extracted from MicrogridScene. Its tests use real GLTFLoader geometry and the scale passed by the real component to a mocked Clone: per-unit shrinking must fail, independently of containment. The placement checks cover real equipment, pads and max PV frames against framing; they do not claim a complete rendered-scene bound including labels, pylons, selection shells or particles. Panel size/height/tilt are shared config values used by rendering and the max-array guard. The PCS=0.3 and PV-x=-40 mutations were rejected and restored byte-for-byte. App tests also cover repeated equipment-button toggle and disabled/enabled tools across viewport failure/retry.

Batch 5 alone re-fits on any canvas size change, including h-dvh height changes. PR #7 merged as `29c81d7`; CI 35779667669 and Pages 35779668154 succeeded. Production includes batch 5 and its batch 6 camera refinement. See `docs/audits/2026-09-20/cc-review.md` for the reviewer/runtime/browser evidence boundary. Batch 6 addresses the resize behavior below.
### Sixth-batch camera resize (2026-09-22, released)

`codex/audit-batch-6-camera-resize` was reviewed as PR #8 and merged to main as `b5cdf58` after PR #7; CI 35780131524 and Pages 35780131948 succeeded. Its functional commit includes `Co-authored-by: Codex <noreply@openai.com>`, which GitHub resolves to the `codex` account. Keep that trailer on future Codex commits in this project. The user-arranged CC review approved the core worktree and independently checked browser behavior, 221 tests / 21 files, lint and Pages-path build. Its follow-up cleanup and the residual-damping regression were verified locally after that review. See `docs/audits/2026-09-22/batch-6.md` and `cc-review.md` there.

`SceneCameraControls` distinguishes automatic overview from manual inspection using OrbitControls `change`: only an actual pose change disables automatic fitting; a click alone does not. Record it immediately so resize during a drag also retains the view. Synchronous programmatic fitting clears the marker after its own `update()` calls. This also covers key-driven camera changes if Drei key events are enabled later; they are disabled now. After inspection, resize updates the camera aspect/projection but preserves position, quaternion, zoom, target, maxDistance and fog. A portrait-to-landscape change therefore keeps the larger portrait zoom-out limit and farther fog start until Full site; this avoids clamping the inspected pose and may show more empty space. Full site flushes old damping, fits the current dimensions and re-enables automatic framing. The residual-damping regression must fail if that flush is removed. An invalid/zero viewport defers the request until a valid fit can be applied. `saveState()` was removed because no code uses OrbitControls `reset()`.

Tests use real Three camera/OrbitControls with a mocked R3F store and Drei mounting/event bridge. They cover height-only resize, portrait/landscape changes, active gestures, no-op clicks, Full site returning to automatic mode and deferred zero-size reset. Production browser checks use real mouse drag/wheel input; no physical-phone touch or browser-chrome test is claimed. Preserving pose can crop content or leave more space after an aspect-ratio change; Full site is the explicit recovery action. Simulation state and history remain outside this controller.

### Seventh-batch scene clarity (2026-09-22, released)

Batch 7 was reviewed in PR #10 from main `9b81a7b` and released as `2989452`. User-arranged CC reviewed the initial worktree and found that Drei `Html` ignores its `pointerEvents` prop outside transform mode; use `style={{ pointerEvents: 'none' }}` so labels pass clicks to Canvas. CC independently rechecked that fix plus HUD stacking, portrait label spacing and the red Grid overload label. The short-landscape media rule lifting PCS at ≤430px viewport height matches the rule CC had tested in-page; CC later confirmed the committed CSS, without repeating the online 640×360 check. Local browser checks cover clicking the PCS label, default-overview label spacing and the overloaded Grid label. See `docs/audits/2026-09-22/batch-7.md` for exact evidence and remaining follow-ups. Node 24.21.0 lint, 223 tests / 21 files and the Pages-path build pass; simulation strategy, state and settlement were unchanged.

PR #10 was merged as `2989452`; main CI 35789638276 and Pages 35789638697 both succeeded on that commit. A live 640×360 browser check confirmed the label separation and showed the already-recorded Solar/legend overlap. The Three vendor chunk warning remains at 724.92 kB. Cumulative value decomposition was addressed in batch 8 / PR #12, the multi-day AUTO evening hold in batch 9 / PR #14, and the displayed tariff threshold in batch 10 / PR #16; Grid-label click behavior and the older `NumericField` Reset draft bug remain open.

`SceneLabel` anchors equipment names to their Three positions through Drei `Html`, but keeps CSS font size stable across full-site camera fits. It is pointer-transparent and below the HUD/drawers. The less important Local Load tag is hidden at ≤420px; the flow legend still names local load. PCS moves upward at ≤480px width or ≤430px height and Grid has a small lateral offset; sampled 320–768px portrait and 640–844px short-landscape default views have no label overlap. Arbitrary manual camera poses are not collision-managed. The original metre-sized label config fields were removed; do not reintroduce world-space font scaling for these tags.

`EnergyFlowLegend` explains source, destination, color and particle direction, using `FLOW_COLORS` shared with `MicrogridScene`. It follows scene-tool visibility so drawers do not compete with it. `EquipmentModelPlaceholder` provides matching silhouettes for all three GLBs while their independent Suspense boundaries wait. `useProgress` displays a separate 3D loading status; the lazy chart has a visible text/skeleton fallback and retains its existing error/retry boundary. The temporary 8-second local asset delay exercised both loading states and their completed state, without changing the production fault server. Keep any future legend claim aligned with `getVisibleEnergyFlows` and route color/threshold behavior.

### Batch 7 scene follow-ups: short-landscape legend and Grid label (2026-09-25, in review)

Branch `claude/sweet-clarke-8xtncd` from main `477293d` addresses the two batch 7 P3 items; it is awaiting user review, and CC was not invoked. It is independent of the concurrent NumericField Reset branch. It changes no simulation, reducer, tick or settlement code. Record and evidence: `docs/audits/2026-09-25/scene-legend-grid-label.md`.

`EnergyFlowLegend` is now a disclosure. A native button inside its `h2` ("Live power routes", `aria-expanded`/`aria-controls`) shows or hides the direction note and color rows. `SimulationViewport` owns the state: the layout default applies until the user toggles. The explicit choice then survives drawers unmounting the legend, and later rotation. `COMPACT_LEGEND_QUERY` (`(orientation: landscape) and (max-height: 430px)`) is mirrored in `index.css`; change both together. There the legend starts collapsed as a chip left of the scene toolbar, on its baseline and at its 54px height. Its details open as a popover above the chip. The popover stops 142px from the top, below the Controls handle, and scrolls rather than reach the HUD. Below 640px width the chip hides its swatches. Taller layouts keep the expanded card, at `top-32` / `sm:top-36`, clearing the Controls handle at ≥640px. There only the toggle takes pointer input. An expanded short-landscape popover is a user-opened overlay and can still cover the Solar label/array.

The GRID NODE label uses the `hitArea` pattern described under R3F scene patterns; do not replace it with DOM event forwarding. The lifted PCS label (≤480px wide or ≤430px high) still selects nothing where its centre is off the skid, as on main. On desktop and at 390×844 a pinned card still covers the Grid label; switching to Grid then needs Close or the toolbar, as on main.

Node 24.21.0 lint, 268 tests / 23 files, the Pages-path build and diff check pass. The Three chunk warning remains at 724.92 kB. `sceneLabelHitArea.test.ts` uses a real Three camera and raycaster. App tests cover keyboard toggling, drawer persistence, the short-landscape default and rotation. Removing the overlay distance, the rectangle bounds, preview pass-through, the compact default or drawer persistence each fails a test. Browser evidence comes from `docs/audits/2026-09-25/browser-check.mjs` and `hover-stability.mjs`, run in headless Chromium 141 with SwiftShader WebGL. That is not a GPU, a phone or real touch. It covers 15 branch and 11 main viewports. The cloud egress policy blocked cdn.jsdelivr.net, so the harness answered troika's default-font requests locally. The verified bundle is byte-identical to the final build.

### Eighth-batch value breakdown (2026-09-23, released)

PR #12 merged as `20dadaf` after user-arranged CC review. Same-commit main CI 35915896134 and Pages 35915896531 succeeded; the live Metrics breakdown and negative-price wording were checked with no browser errors. Node 24.21.0 lint, 230 tests / 21 files, and the Pages-path build passed. Each settlement substep splits BESS discharge value into export revenue, actual avoided imports, and an assumed value for restored load. The last component is not cash flow; all three can be negative with negative tariffs. Tests independently derive the first two load-side components from capped imports and overload reduction, including a 72-hour overload case. See `docs/audits/2026-09-23/batch-8.md`. Multi-day AUTO reserve policy remains a separate product decision; CC observed AUTO can exhaust SoC before a late peak under sustained 150% demand.

### Ninth-batch evening-peak AUTO release (2026-09-24)

The user chose to prioritize the evening peak. PR #14 (`dd3df1d`) keeps the existing 40% overnight charge target through the shoulder only when `autoPolicy.ts` estimates that peak delivery plus avoided overnight recharge beats 0–40% shoulder self-consumption. The closed-form gate uses charge/discharge efficiency, the 12% peak reserve, and the small residual left by the 0.25-hour peak-pacing floor; under 80/150 €/MWh night/shoulder rates its peak threshold is about €178.78/MWh. A simpler reserve-only gate would have lost €750–€162 over three days at peak prices €177.5–178.5. These comparisons assume a full shoulder discharge and unconstrained peak cycle; arbitrary load, PV and PCC settings can differ. Above 40%, shoulder deficit discharge and PV-surplus charging remain available. At 18:00 the existing peak pacing and 12% reserve apply; manual modes and settlement formulas are unchanged. The 40% shoulder floor is an energy event, so do not average discharge past it across a tick.

User-arranged CC reviewed the first candidate and identified the loose price gate. Its second review confirmed the revised formula, 15 forced-hold-versus-no-hold comparisons and the pinned three-day results. Main CI 35976088474 and Pages 35976088804 passed on `dd3df1d`; the live Controls panel shows the 40% peak-entry target and tariff-hurdle explanation with no observed console errors. Node 24.21.0 lint, 257 tests / 22 files, Pages-path build and diff check pass. A pinned `b824258` 72-hour comparison starts and ends at 06:00/40% SoC: default-rate Project demo value rises about €100.8k at both 100% and 150% load. At 150% load, €85.2k of the candidate total is assumed restored-load value; excluding it, the default-rate change is only about +€15.6k and at a €200/MWh peak it is −€36.2k despite a positive displayed demo-value change. This is not a cash-profit guarantee. CC also found a 20 MW BESS transfer-limit case where the tariff-only gate chooses hold despite a small modeled loss; keep the unconstrained-cycle assumption explicit. See `docs/audits/2026-09-23/batch-9.md` for the formula, price scan and review entry points.

### Tenth-batch tariff-threshold display release (2026-09-24)

`getEveningPeakHoldThresholdEurMwh` is the shared closed-form threshold used by both the AUTO hold decision and the Controls explanation. The strict peak-above-shoulder guard is included in the returned threshold. Display to two decimal places with "about"; make the policy decision using the unrounded number. The UI warns if no peak rate within the editable −500 to 1000 €/MWh range can clear it. This is a tariff-only heuristic, not a load/PV/PCC-aware value prediction; preserve the transfer-limit and restored-load valuation caveats above. PR #16 merged as `c4a467a` after user-arranged CC review and small follow-ups; main CI 35982356235 and Pages 35982356587 passed. The live default-rate panel displayed about €178.78/MWh and the PCC caveat with no observed console error. See `docs/audits/2026-09-24/batch-10.md`.
